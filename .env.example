# Keep real credentials outside source code.
# Fill these in your shell or local .env loader; do not commit secrets.

IFIND_MCP_URL=https://api-mcp.51ifind.com:8643/ds-mcp-servers/hexin-ifind-ds-stock-mcp
IFIND_MCP_AUTHORIZATION=

# Tool names depend on the MCP server's exposed tool schema.
# Configure them after listing/confirming available iFinD MCP tools.
IFIND_MCP_QUARTERLY_TOOL=
IFIND_MCP_HIGH_FREQUENCY_TOOL=
